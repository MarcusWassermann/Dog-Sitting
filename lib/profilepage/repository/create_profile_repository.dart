// repositories/create_profile_repository.dart

class CreateProfileRepository {
  Future<void> saveProfileToFirestore(Map<String, dynamic> profileData) async {
    // Hier wird das Benutzerprofil in Firestore gespeichert
  }
}
